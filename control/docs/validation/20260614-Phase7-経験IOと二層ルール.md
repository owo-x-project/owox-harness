# Phase7 スライス4 経験 export/import + 二層ルール + 状態適応の検証

## 検証対象

Phase7 スライス4 (`docs/decisions/20260614-Phase7-経験IOと二層ルール.md`)。
経験 export/import (汎用のみ)・二層ルール (固定 rules.md + 成長層 practices.md)・canon.propose (ブランド変更提案)・状態適応の再起動回帰。

- core: model.rs (Practices/Practice 型・Canon へ practices)、load.rs (practices.md 読込)、practices.rs (practice.add)、proposal.rs (canon.propose)、experience.rs (export/import + 秘密走査)、decay.rs (run_practice_decay)、hook.rs (session_start_context へ practices 注入 + 回帰テスト)、lib.rs (公開)
- mcp: serve.rs (practice.add / canon.propose / experience.export / experience.import tool、verify.run と next で practice decay 結合)

検証する owox の版: 本作業ツリーの release ビルドを `/usr/local/bin/owox` へインストール。target サンドボックス (実 target の一時コピー) で MCP stdio/hook 確証。

## 結果

### 単体 (cargo)

- cargo test 205 passed (191→205。Practices 解釈・canon.add/canon.propose・experience export/import・run_practice_decay・session_start の maintenance/practices 注入を追加)
- clippy clean・fmt clean

### MCP stdio (サンドボックス)

- practice.add: practices.md へ `- 20260614: ...` を追記 + 来歴記録 (status ok)。成長層 = AI 可
- canon.propose (target=rules): canon を変えず open gate (来歴 20260614-Propose-change-to-rules) + needs_human。理由文が「経験で固定でない学びは practice.add」へ誘導
- experience.export: 束を out_path へ書き出し needs_human。skills/tidy/SKILL.md + scripts/run.sh + practices.md を出力・memory.md は除外 (data.excluded に memory/tests/owox.toml/brand/rules/glossary)
- experience.export (秘密入り skill): status failed・束を 1 file も書かない。「skills/tidy/SKILL.md [credential-assignment]」と種別だけ報告 (値は漏らさない)
- experience.import (別サンドボックスへ): skill を draft で取り込み (owox.toml 無し)・practices を 1 件マージ (日付保持)・status ok。「Re-run their tests and promote them here」で移植先の再評価へ誘導

### hook (session-start): 状態適応の再起動回帰 + 成長層ライブ注入

- state.toml = maintenance の別サンドボックスで owox hook session-start を新プロセスで実行 → 「Phase: maintenance.」と maintenance 振る舞い案内 (Keep changes small ... add a regression test) を注入。phase は state.toml に永続し session 開始ごとに再読込される (再起動後も効く)
- 成長層 practices が「## Practices」節としてライブ注入される (静的 AGENTS.md でなく session_start の live 経路・glossary 用語名と同じ)

### 二層の線引きの確認

- 固定層 (brand/rules/glossary) = 人間が握る個性。AI は canon.propose で提案のみ・直書きしない。export しない
- 成長層 (practices) = AI が経験で育てる運用指針。practice.add で追記・鮮度 decay (stale-practice) で見直し合図・export 可
- 経験束 = skill (SKILL.md + scripts) + practices。memory/tests/owox.toml はドメイン固有・ライフサイクル状態として型で除外

### 統一 canon 編集モデル (追補・拡張)

人間判断で brand/rules/practices/glossary を 1 つの編集モデルへ統一し、MCP ツールを集約した。

- canon.add (AI 直接 + 来歴): brand value (section=values)・glossary ("用語: 定義")・practices いずれも status ok で各ファイルへ追記。brand.md の Values へ追記・glossary に用語・practices.md に指針が入った
- canon.propose (変更・削除は人間ゲート): glossary 対象で needs_human + gate.kind="canon"。canon は変えない
- rules の section=irreversible は add で reject (機械強制・構造化のため propose 経由 = 安全側)
- tools/list は canon.add / canon.propose / glossary.lookup の 3 つだけ。旧 glossary.add/update/remove・practice.add・旧 canon.propose は撤去 (scattered → 統一・最小コンテキスト)
- 内部は既存 per-target 関数 (glossary::add の重複検査/Forbidden 保護・practices::add) を再利用。glossary の変更/削除は canon.propose へ移し glossary::update/remove/propose_change を撤去
- cargo は 205 passed (clippy/fmt クリーン)

### 清潔さ

- 実 target は未接触 (一時コピーで検証)・git クリーン (277b06b 未変更)・サンドボックス/束は破棄済み
- control 差分は意図どおり (model/load/decay/hook/lib + practices/proposal/experience 新設・serve 編集 + 設計・検証文書)

## 失敗と再現手順

なし (全観点が期待どおり)。

## 未確認事項

- 実機 Codex の対話検証 (全スライス後にまとめる): AI が成長層を自発的に育てるか・canon.propose と practice.add を正しく使い分けるか・経験を別プロジェクトへ持ち込む流れが自然か・秘密走査が誤検出で妨げないか
- 成長層 practices の要約圧縮 (肥大時)・rules/instructions 以外の経験 kind・glossary 用語照合による domain 漏れ検査 (秘密走査で代替・将来)・export の item 選択
- run_practice_decay の stale-practice が verify.run/next に出ること自体は単体で担保 (実機の溜め込み運用は対話検証で)

## 次の推奨

- `commit`: スライス4 を区切る
- 全スライス (1〜4) 揃ったので実機 Codex 対話検証 → 粗を是正 → M3 達成宣言
