# Phase6 スライス4 レビュー枠組み (routable) の検証

## 検証対象

Phase6 スライス4 (`docs/decisions/20260614-Phase6-レビュー枠組み.md`)。
判断型レビューの観点・進め方を owox 標準として持ち、変更内容で機械選択する (routable)。

- core: review.rs (Lens/Applicability・standard_lenses・select_lenses・load_lenses・review.toml・review_lenses_envelope)、commands.rs (review 入口本文)、quality.rs (glob_to_regex を pub(crate) 化し再利用)
- mcp: files.rs (changed_files = git diff)、serve.rs (review.lenses tool・get_info)

検証する owox の版: 本作業ツリーの debug ビルド。

## 結果

### 単体 (cargo)

- cargo test 146 passed (140→146。観点集合・select_lenses の Always/Paths・依存マニフェスト・review.toml 追加上書き・envelope を追加)
- clippy clean・fmt clean

### MCP stdio (review.lenses が変更内容で観点を機械選択)

review.toml に migration-safety (paths=migrations/**) を置き、変更を変えて確認した。

- 変更が src のみ → 普遍6観点 (correctness/design/security/plan-alignment/requirement/pruning)。dependency・migration-safety は出ない
- 依存マニフェスト (Cargo.toml) 追加 → dependency 観点が加わる (言語横断の既定トリガ)
- migrations/ 追加 → プロジェクトの migration-safety が効く

変更内容で適用観点が機械的に振り分かることを確認。Phase8 のルータが同じ select_lenses 契約を読み subagent とティアを振り分ける土台ができた。

### 枠組みの確認

- review 入口本文に進め方 (review.lenses 取得 → verify.run 取り込み → 各観点で指摘 → 事実性確認 → 敵対的再確認) と庭師の安全規則 (剪定は提案・削除基準+verify+来歴・不可逆は人間) が入る。ツール非依存 (CLI 名なし)
- 観点は owox 標準・具体バーは rules/quality・追加と上書きは review.toml という層分け

### 清潔さ

- target repo は未変更 (サンドボックス検証)・後片付け済み
- control 差分は意図どおり (review.rs/files.rs 拡張・commands/serve/quality/lib 編集 + 設計・検証文書)

## 失敗と再現手順

なし (全観点が期待どおり)。検証中、再ビルド前の stale バイナリで review.lenses が tool not found になったが cargo build で解消 (実装の問題ではない)。stdio 検証の前に必ず cargo build する。

## 未確認事項 (後の一括対話検証へ)

- 実機 Codex の対話: review が review.lenses で適用観点を取り、verify.run を取り込み、機械検証済みを二重に裁かず多視点+敵対レビューを回すか。剪定提案が盲目削除へ走らないか。依存追加で dependency 観点が効くか
- subagent による並列・敵対実行とリスク/規模でのティア振り分け (Phase8)
- repo 全体の腐敗検知 (Phase7)
- スライス1〜3 の観察 (来歴 ID 長) は継続

## 次の推奨

- `commit`: スライス4 を区切る
- Phase6 完了の記録: `docs` (roadmap へ Phase6 完了・M2 達成を反映)
