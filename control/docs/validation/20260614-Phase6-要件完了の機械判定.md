# Phase6 スライス2 要件完了の機械判定の検証

## 検証対象

Phase6 スライス2 (`docs/decisions/20260614-Phase6-要件完了の機械判定.md`)。
要件↔テスト trace と要件完了の機械判定。完了3区別の完成 (旗「検証可能な正本」)。

- core: requirement.rs の met 判定 (judge) ・書き込み時の検査名照合 (check_known) ・needs_trace、verify.rs の run_verify 拡張 (requirements を受け per-requirement と集約を返す)
- mcp: verify_run が要件を読み判定、link_verification / requirement.create が config 検査名へ照合、next が trace 欠落要件を先回り

検証する owox の版: 本作業ツリーの debug ビルド。

## 結果

### 単体 (cargo)

- cargo test 132 passed (122→132。judge 5 種・needs_trace・照合 2 種・run_verify の met/未 trace を追加)
- clippy clean・fmt clean

### MCP stdio (brand + config に検査 unit=true を置いたサンドボックス)

accepted 要件 (基準 1・検証 link なし) を作り、一連を流して確認した。

- next: 未 trace の accepted 要件を「Requirements needing a verification trace」で先回り表示 (検査を実行しない静的信号)
- verify.run (未 trace): completion.requirement=needs_human、requirements[].met=needs_human、reason に欠落基準番号
- link_verification (未知検査 "ghost"): failed。利用可能な検査名 (unit) を添えて弾く (dangling を作らせない)
- link_verification (実在検査 "unit"): ok
- verify.run (trace 後): completion.requirement=**met** (機械判定)、requirements[].met=met。封筒は needs_human (作業完了は人間確認) だが requirement が needs_human → met へ動き、旗が立つ
- next (trace 後): trace 欠落が消える

### 旗の確認

完了3区別の requirement が、第1段階の一律 needs_human から、trace のある要件で機械判定 met へ移った。trace の無い要件は needs_human のまま (ハイブリッド)。完了3区別が完成し、旗「検証可能な正本」が現物で立った。

### 清潔さ

- target repo は未変更 (サンドボックス検証)・後片付け済み
- control 差分は意図どおり (requirement.rs / verify.rs / serve.rs / lib.rs + 設計・検証文書)

## 失敗と再現手順

なし (全観点が期待どおり)。

## 未確認事項 (後の一括対話検証へ)

- 実機 Codex の対話: AI が「未 trace を next で気づく → 検査を config へ → link_verification → verify.run で met」の流れを自発的に回すか、未知検査の弾きで迷わず config へ誘導されるか
- スライス1 の観察 (来歴 ID が長い) は継続
- スライス3 (quality.toml)・スライス4 (多視点レビュー枠組み)

## 次の推奨

- `commit`: スライス2 を区切る
- スライス3 へ: `plan` または `design` (quality.toml の適応度関数)
