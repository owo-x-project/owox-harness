# 完成ロードマップ

## 状態

進行中。Phase 8 完了・M4 達成 (経験を厚くする: 調査知識層・スキルテスト是正の契約 lint・パターンからスキル育成・canon 編集の統一維持)。cargo 245 passed・MCP stdio・hook で機械確証、実機 Codex 対話検証も完了 (調査知識の直読みせぬ往復・古い調査の stale 警告・契約 lint で壊れたスキルが draft・canon.propose の人間ゲート・canon.add 未知 section フィードバック)。検証中の粗2件を是正 (床 routing に knowledge 行・routine の distinct フィルタ)。Phase 9 (規模・移植・方向付け) 進行中: 性質軸適応・ブランチ記憶・MCP コンテキスト削減・自動承認パス再設計・オーケストレーション (Codex は custom agent_type spawn 不可と実機判明しフォールバック採用)・マルチ CLI 生成 (Claude Code 第2対象)・既存コードからの canon 逆生成 (canon.detect) を実装・機械確証 (cargo 369 passed・clippy 0)。Phase 9 実装は一巡。残は実機/対話検証 (人間関与)。

## 位置づけ

要件の実装順 (`docs/requirements/20260611-core-要件.md`) を、検証できる成果で区切るフェーズへ具体化する。

- 何を作るかの正本: `docs/requirements/`
- なぜその順かの正本: `docs/decisions/`
- 本書: いつ、どの順で、誰が何を確認するか

## 現在地

- Phase 10 (条件付き機能と配布) 配布着手・実装。owox-harness 自身の配布インフラ (モノレポ root の release workflow・native runner 4種・SHA256SUMS・setup.sh/install.ps1・版 0.1.0・owox --version) と対象プロジェクト向け release.toml (任意正本・policy/version/artifacts/checks・release.check MCP tool) を実装。cargo/clippy/fmt/shellcheck で機械確証。残は実 tag push 配布実走と別環境導入 (人間関与)。eval とチーム権限は別スライス。詳細 `docs/decisions/20260621-Phase10-配布とrelease正本.md`・`docs/requirements/20260621-Phase10-配布と条件付き機能.md`
- Phase 9 (規模・移植・方向付け) 進行中・実装一巡。性質軸適応・ブランチ記憶に続き、MCP コンテキスト削減 (生成後除去で title/default 削減・$schema 保持)・自動承認パス再設計 (architecture 軸導出+profile/session の2系統)・自律度根本方針 (最初を厚く+3安全弁)・オーケストレーション (正本 agents.toml・床の能動提示・Codex は custom agent_type spawn 不可で組込み型+prompt 注入のフォールバック採用)・マルチ CLI 生成・canon 逆生成を実装。cargo 369 passed・clippy 0。残は実機/対話検証 (人間関与)。詳細 `docs/requirements/20260620-Phase9-自律度とオーケストレーション.md`・`docs/decisions/20260621-Phase9-マルチCLI生成.md`・`docs/decisions/20260621-Phase9-canon逆生成.md`・`docs/validation/20260620-Phase9-オーケストレーション実機.md`
- マルチ CLI 生成 (実装済): Target 抽象を流用し registry へ追加。第2対象 Claude Code (`.claude/settings.json` hooks・`.mcp.json`・skills を MergeJson/Overwrite・床は SessionStart 注入・MultiEdit を層ゲートへ)。共有 JSON 設定は MergeJson で人間設定を壊さない。詳細 `docs/decisions/20260621-Phase9-マルチCLI生成.md`
- 既存コードからの canon 逆生成 (実装済): canon.detect。層ディレクトリから guarded core / free edge 層と依存方向境界、migrations/terraform/k8s 痕跡から不可逆操作の detect 案を逆生成し、貼れる TOML/markdown 断片+根拠を返す。proposal のみ人間ゲート (canon は書かない)。profile.detect と DetectSignals 共有・床 routing と kickoff へ導線。詳細 `docs/decisions/20260621-Phase9-canon逆生成.md`
- Phase 9 追補4機能 (経験層スケール・glossary 別名・GitHub 同期・kickoff 束ね) 実装+実機 Codex 対話検証 完了 (2026-06-21)。床降格と practice.lookup・別名解決と canon.add 弾き・task external 往復と issues skill・kickoff の draft 相乗りを確認。粗4件 (skill 発見性のモデル依存・日付なし practice 脱落・guardrails_draft の path 形式・gh 失敗の skill 記載) は decisions の見直し条件へ。詳細 `docs/decisions/20260621-Phase9-経験層スケールとGitHub連携とkickoff束ね.md` の実機検証節・`docs/handoff/20260621-経験層スケール他4機能の対話検証.md`
- Phase 8 (経験を厚くする) 完了・M4 達成。cargo 245 passed と MCP stdio/hook で機械確証、実機 Codex 対話検証も完了
- 4 スライスで縦貫: (1) 調査知識層 (`.owox/knowledge/`・1調査1ファイル・調査日/ソース/要約・経過日数鮮度で stale-knowledge・オンデマンド読みで床へ非注入・supersede 専用・経験 export 除外・add で秘密走査) (2) スキルテスト是正 (契約 lint を全スキル共通の最低ゲートに・SKILL.md が参照する scripts/<name> 実在 + tests 実行ビットを検査・テスト合格でも欠落で draft) (3) パターンからスキル育成 (使用履歴 `.owox/usage.log` を MCP の call_tool 単一集約点 + PreToolUse で記録・名前のみ・頻出する distinct 多段手順を next/verify.run で助言・育成は既存ライフサイクルへ) (4) canon 編集の統一維持 (分割せず canon.add/propose の説明簡潔化 + 未知 section で valid_sections フィードバック)
- 実機対話で粗2件を是正: 床 routing に knowledge 行を追加し調査記録の practices 誤誘導を解消・routine 提案に distinct な name 2種以上を要求し Bash 連打ノイズを排除
- 詳細: `docs/decisions/20260616-Phase8-調査知識層.md`・`20260616-Phase8-スキルテスト是正.md`・`20260616-Phase8-パターンからスキル育成.md`・`20260616-canon編集の統一維持.md`、`docs/validation/20260617-Phase8-対話検証と粗の是正.md`、要件は `docs/requirements/20260616-Phase8-経験を厚くする.md`
- Phase 6 (検証を閉じる) 完了・M2 達成。コア完成・旗が立つ。cargo (146 passed) と MCP stdio で機械確証、実機 Codex の対話検証も完了 (粗2件を是正、`docs/validation/20260614-Phase6-対話検証.md`)
- 4 スライスで縦貫: (1) requirements 正本層 + tool (`.owox/requirements/`・1件1ファイル・状態はライフサイクルのみ・充足は導出) (2) 要件↔テスト trace + 要件完了の機械判定 (検証 link は config 検査名・書込時照合で dangling 不能・accepted ごと met/needs_human/failed・completion.requirement が met へ移り旗が立つ・trace 無しは needs_human のハイブリッド) (3) quality.toml 適応度関数 (行数予算・禁止パターンで層境界、glob は regex 変換、commit で phase 適応・verify.run は報告、循環/複雑度は検査委譲) (4) レビュー枠組み (routable。観点は owox 標準・変更内容で機械選択 review.lenses・庭師の剪定観点と安全規則・実行モデル非依存で Phase9 が同契約を消費)
- 詳細: `docs/decisions/20260614-Phase6-requirements正本層.md`・`20260614-Phase6-要件完了の機械判定.md`・`20260614-Phase6-quality適応度関数.md`・`20260614-Phase6-レビュー枠組み.md`、`docs/validation/20260614-Phase6-*.md`、要件は `docs/requirements/20260614-Phase6-検証を閉じる.md`
- Phase 5 (スキルと入口) 完了・M1 達成。実機 Codex の対話で確証
- スキルライフサイクル (draft/registered/promoted・テスト合格ゲート・skill.register/promote/remember/list) ・入口=薄い skill (kickoff/next/status/decide/verify/review/task/skill/handoff の 9 個・明示起動) ・setup (生成 + 検査) ・glossary 動的化 (CRUD tool・用語名 session 注入) ・canon 直読み禁止を実装
- 実機対話で粗を発見し是正: 読み (next/context) を resource から tool へ一本化・入口を 1 tool へ強束ね・openai.yaml を Codex 正式スキーマ (policy/interface) へ・応答言語を正本設定+注入で追従・skill ライフサイクルの入口追加。是正後に $next/$verify/$skill・/skills 表示・昇格後の暗黙起動・日本語応答を実機確認
- 後回し: Stop block が未コミットツリーで毎ターン鳴るノイズ (安全側・別途設計)
- 詳細: `docs/decisions/20260613-Phase5-スキルと入口.md`・`20260613-Phase5-実機検証の是正.md`、`docs/validation/20260613-Phase5-スキルと入口.md`
- Phase 4 (MCP 中核) 完了。実機 Codex の stdio と対話で確証
- owox serve + resource (owox://context / owox://next) ・tool 層 (decision.record / gate / verify.run / state.set / task 群 + note / update) ・記録層 (`.owox/decisions` ・ `.owox/tasks`) ・共通返り値 (封筒) を実装
- commit/stop を機械ゲートへ格上げ (commit は検査再実行で verify 失敗を deny、maintenance は未承認 open gate も block、stop は変更ありターンのみ誘導)
- 対話検証で見つけた粗を改善: Stop は変更なしターンで黙る・用語注入の session 重複排除・依存先検証・一時メモは task.note・tool 発見性を get_info で底上げ。6 観点を実機再確認
- 詳細: `docs/decisions/20260613-Phase4-MCP実装.md`・`20260613-Phase4-tool記録層.md`、`docs/validation/20260613-Phase4-MCP-resource読取.md`・`20260613-Phase4-tool記録層.md`、`docs/handoff/20260613-Phase4対話検証で見つけた粗の改善.md`
- Phase 3 (機械強制 hook) 完了。実機 Codex で確証
- 不可逆操作を PreToolUse で決定的に deny (owox 同梱の既定 ∪ rules.md の detect 正規表現)。入出力契約は一次情報で確定
- commit/stop は誘導まで (commit 前の一行リマインダ・Stop の 1 回継続)。機械ゲート化 (verify 状態・判断記録で block) は状態を持つ Phase 4 へ
- 差分要約は常時 push せず Phase 4 の owox:// (中身はオンデマンドで引く) へ。理由はコンテキスト汚染回避・最小コンテキスト
- 作業メモ (work-note) は保存・取り出しの記録層が要るため Phase 4 と一体で作る
- hook は薄いシェルを廃し `owox hook <event>` を直接呼ぶ
- 詳細: `docs/decisions/20260612-Phase3-hook実装.md`、`docs/validation/20260612-Phase3-不可逆deny.md`・`20260612-Phase3-commit-stop配管.md`
- Phase 2 (正本の型確定) 完了
- 正本を Markdown・英語固定へ転換 (TOML 編集の苦痛を受けた決定。型は読込後に Rust で検証)
- brand (Goal/Values/Principles/Non-goals/Success criteria/Style/Glossary) ・rules ・context ・targets の型を確定
- 段階的開示を設計 (常時=気づき / 関連時 / オンデマンドの3層)。届ける実装は Phase 3/4
- ルート指示ファイル (AGENTS.md) を本実装。context/targets は型確定のみで生成物へは出さない
- Phase 1 で実機確証済み (SessionStart hook 発火・additionalContext 注入)。MCP resource 読取は Phase 4

## 完成の定義

- コア完成: 旗「検証可能な正本」が現物で立つ (Phase 6 末)
- 全機能完成: 規模・移植・条件付き機能まで揃う (Phase 10 途中)
- 製品完成: 配布でき、他者が導入できる (Phase 10 末)

## Phase 0: 配管実証 (完了)

- ゴール: アーキの背骨が動くか
- 確認できたこと: 正本 TOML を型で読み、Codex 向け生成物を出し、owox 実行ファイルが hook 補助として動く

## Phase 1: 実機で土台を確証する

参照: `docs/decisions/20260611-第1対象CLI.md` の注意

- ゴール: 推測で作った部分、特に hook 発火が、実際の Codex CLI で動くと確かめる
- 作るもの: 検証用 target repo、owox 実行ファイルの手動配置手順
- 人間がすること: Codex CLI を起動しセッションを始め、注入された文脈を AI が認識するか会話で確かめる
- 確認方法: session 開始 hook が発火するか / additionalContext の文脈が AI へ届くか / MCP resource (owox://) を読めるか / 再生成で壊れないか
- 確認できること: 仕組みとして本当に成立するという確証。失敗したら設計を見直す (要件の最優先検証項目)
- 完了の目安: hook 発火・文脈注入・resource 読取が実機で動いた

## Phase 2: 正本の中身を確定し本生成にする

参照: `docs/decisions/20260611-target-harness内容.md`

- ゴール: 仮置きの正本構造を正式に決める
- 作るもの: brand (用語集・表記文体を追加) / rules / context / targets の型確定 (現状 brand のみ)。ルート指示ファイルの文面を本実装。quality.toml は第1段階要件の対象外のため Phase 6 で扱う
- 人間がすること: 各正本に何の項目を持たせるか決める。自分の target repo で正本を書き、編集しやすいか確かめる
- 確認方法: 長文ルールを TOML で編集しやすいか / 生成された指示文が短く的確か / 必要な情報だけ出ているか
- 確認できること: 人間が正本を書きやすいか、生成物が最小コンテキストか
- 完了の目安: 正本の項目構造が固まり、編集が苦痛でない

## Phase 3: 機械強制 (hook) を完成させる

参照: `docs/decisions/20260611-制御方針.md`

- ゴール: 危ない時と完了時に必ず止まるを機械で効かせる
- 作るもの: hook 5種 (session 開始 / 編集前の安全・可逆性チェック / commit・stop 前の完了検証 / 差分要約 / 作業メモ)。可逆性境界の決定的ブロック
- 人間がすること: どの操作を不可逆 (人間確認必須) とするか方針を決める。危険操作を試し止まるか確かめる
- 確認方法: 不可逆操作の前で止まるか / commit 前に検証なしを弾くか / 誤発火で作業を妨げないか
- 確認できること: 人間が重要判断を握ったまま止まれるという安全性の核
- 完了の目安: 止めるべき所だけ確実に止まる

## Phase 4: MCP 中核を立てる

参照: `docs/decisions/20260611-MCP設計.md`、`docs/decisions/20260613-Phase4-MCP実装.md`、`docs/validation/20260613-Phase4-MCP-resource読取.md`

進捗: 縦貫スライス (owox serve + resource) を実機 Codex で確証。
Codex は MCP resource を read_mcp_resource ツールでモデルへ渡すと判明 (Phase 1 保留の resource 読取が解消)。
実装手段は公式 SDK rmcp を採用。
tool 層 (decision.record / gate.list / gate.approve / verify.run) ・記録層 (`.owox/decisions/`) ・
共通返り値 (封筒) ・計算 resource owox://next を実装し、実機 stdio で全 tool 呼び出しと完了3区別を確証
(`docs/decisions/20260613-Phase4-tool記録層.md`、`docs/validation/20260613-Phase4-tool記録層.md`)。
commit/stop hook の機械ゲート化を実装・実機確認: commit は config の検査を再実行し verify 失敗なら deny (AI が verify をサボっても効く歯)、未承認 open gate は警告、不可逆は deny。stop は誘導 + open gate 件数を表面化。検査再実行方式で状態を持たず古い結果で通さない (`docs/decisions/20260613-Phase4-tool記録層.md`)。
state.toml + state.set で状態適応も実装: 保守状態 (maintenance) では未承認 open gate も commit を block へ格上げ。phase はゲートだけでなく振る舞い案内 (initial=大規模改修推奨 / maintenance=保守) を session 開始で注入し AI の動き方も変える。owox://state は「読めるだけで行動に効かない」ため撤去。state の per-scope 分割は将来。
検証可能タスク層 (`.owox/tasks/`) も実装: done に検証必須 (task.close は検査通過まで閉じれない) ・drop は理由を来歴へ・ready は前提 done。owox://next に ready タスクを統合。腐敗検知の適応度関数は Phase 7。
owox://verification は作らない (owox://state 撤去と同じ理由。verify.run が真実、保存ビューは古くなる罠)。
Phase 4 完了。実機 Codex の対話で記録・検証・gate・タスク・状態適応が自然に回ると人間が確証。対話で見つけた粗 (Stop 素通り・用語重複排除・依存先検証・decision 過剰記録・tool 発見性) も改善し 6 観点を再確認 (`docs/handoff/20260613-Phase4対話検証で見つけた粗の改善.md`)。残課題は maintenance 振る舞い案内の再起動実見と本番 hook 環境の git 経路観察 (任意)。
多視点レビューは owox 標準 (ハーネス指定)、剪定は庭師として一級概念 (`docs/decisions/20260613-庭師-剪定.md`、Phase 7+8)。

- ゴール: 主入口 (MCP) を動かす
- 作るもの: owox serve (MCP サーバ)、resource (進行的開示)、tool (harness 生成 / decision 記録 / verify 完了3区別 / gate 人間判断点)、共通返り値
- 人間がすること: AI に作業させ、判断記録と検証が自然に回るか体感する。gate で人間に戻る点を確認
- 確認方法: AI が resource で必要分だけ読むか / 判断が .owox/decisions/ に残るか / verify が完了を3区別して返すか (この段階の要件完了は人間判断として返る)
- 確認できること: 判断・経緯が機械記録される、失敗が構造化して返るという動的部分の価値
- 完了の目安: AI 作業のなかで記録・検証・人間ゲートが回る

## Phase 5: スキルと入口を揃える (第1段階完成) — 完了・M1 達成

参照: `docs/decisions/20260611-ショートカット.md`、`docs/decisions/20260611-設計原則.md`、`20260613-Phase5-スキルと入口.md`、`20260613-Phase5-実機検証の是正.md`

- ゴール: AI が技を育て、人間が入口を持つ
- 作ったもの: スキルライフサイクル (作成 → テスト合格ゲート → 登録 → 昇格 → 改善、経験メモリ)、入口=薄い skill 9 個、setup (生成 + 検査)、glossary 動的化、canon 直読み禁止、応答言語追従
- 確認できたこと (実機 Codex 対話): テスト合格 skill だけ登録・昇格後の暗黙起動・$入口が owox tool を確実に引く・/skills 表示・setup で別 target へ導入・日本語応答
- 完了: 第1段階完成。Codex で読む・止まる・記録する・育つが一通り回ると確証 (M1)。残ノイズ (Stop) のみ後回し

## Phase 6: 検証を閉じる (コア完成・旗が立つ) — 完了・M2 達成

参照: `docs/requirements/20260614-Phase6-検証を閉じる.md`、`docs/decisions/20260614-Phase6-*.md`、`docs/decisions/20260611-品質保証.md`

- 完了 (2026-06-14): requirements 正本層・要件完了の機械判定・quality.toml 適応度関数・レビュー枠組み (routable) を縦貫実装。完了3区別が完成し旗「検証可能な正本」が現物で立つ。cargo (146 passed)・clippy・MCP stdio で機械確証
- 対話検証完了 (2026-06-14): 実機 Codex で S1〜S4 を確認。AI が canon 直読みせず tool で読む・未知検査 link を弾かれ正しく寄せる・保守 phase の助言で違反を自発修正・変更内容で観点をルーティングを確認。粗2件 (quality 走査が未追跡を見ない・changed_files 偽カウント、ともに files.rs) を是正。詳細 `docs/validation/20260614-Phase6-対話検証.md`
- ゴール: 要件が満たされたかを機械で判定できるようにする
- 作るもの: requirements/ (要件・受け入れ基準・検証リンク)、quality.toml の機械検証、品質保証 (計画整合 / 要件とテストの追跡 / 多視点レビュー)
- 人間がすること: 自分の target repo で要件を書き受け入れ基準を定める。AI の実装が要件とつながるか確認
- 確認方法: 要件とテストの追跡が成立するか / 要件完了が機械判定へ変わるか / 品質バー違反を弾くか
- 確認できること: 完了3区別の完成。旗「検証可能な正本」が現物で立つ
- 完了の目安: コア完成。製品の中心価値が成立

## Phase 7: 腐らず成長する

参照: `docs/requirements/20260614-Phase7-腐らず成長する.md`、`docs/requirements/20260611-core-要件.md` の B群、`docs/decisions/20260611-タスク管理.md`

- Phase 7 完了・M3 達成 (2026-06-16)。4スライス (腐敗検知の中核 / コード・repo へ拡大 / 測定可視化+ブランド検証 / 経験 IO+二層ルール+状態適応) + canon 編集の核心是正 + コンテキスト配信の再設計を実装・機械確証・実機 Codex 対話検証まで完了
- コンテキスト配信の再設計 (完了): AGENTS.md 廃止し床コンテキスト (向き付け・意図ルーティング・Vision・全体スタイル・状態・用語名・practices) を SessionStart 注入へ一本化。意図ルーティング表で tool 名・用語を言わない曖昧リクエストでも正しい tool へ流れる。rules を `## Rules` へ集約し語トリガ push (policy_injection)・rules.lookup・編集前 push の3経路。圧縮復元は PostCompact が context 注入非対応 (additionalContext 未サポート・実機判明) のため SessionStart matcher を startup|resume|compact へ広げて担う。実機 Codex 6 プロンプトで意図ルーティング・用語 push・canon 直読み deny・rules 語トリガ・剪定ルーティング・承認自動適用・圧縮後の床復元を確証。詳細 `docs/decisions/20260616-Phase7-コンテキスト配信の再設計.md`・`docs/validation/20260616-Phase7-コンテキスト配信の再設計.md`
- canon 編集の核心是正: canon.propose op=remove/replace → 承認時 owox 自動適用・gate.approve の destructive 確認・生成物非編集。詳細 `docs/decisions/20260616-Phase7-canon変更の自動適用と承認の機械強制.md`・`docs/validation/20260616-Phase7-対話検証と粗の是正.md`
- スライス3 = トークン量の軽い推定表示 (CJK 対応・新規依存なし) と秘密走査 (irreversible 方式・値は漏らさない) を context 出力へ、ブランド検証 (glossary.md Forbidden の禁止語を run_brand が QualityViolation kind="brand" へ・表記は boundaries 流用)。詳細 `docs/decisions/20260614-Phase7-測定可視化とブランド検証.md`・`docs/validation/20260614-Phase7-測定可視化とブランド検証.md`
- スライス4 = 経験 export/import (経験束 = skill SKILL.md+scripts + 成長層 practices・固定層/memory/tests/owox.toml は型で除外・本文秘密走査・export はクリーン書出+needs_human/秘密 failed・import は draft+practices マージ)、二層ルール (固定層 brand/rules/glossary は人間・成長層 practices は AI が practice.add で育て鮮度 decay・session_start ライブ注入)、canon.propose (AI が固定層変更を提案→人間ゲート・欠落を埋める)、状態適応の再起動回帰。詳細 `docs/decisions/20260614-Phase7-経験IOと二層ルール.md`・`docs/validation/20260614-Phase7-経験IOと二層ルール.md`
- スライス1 = タスク腐敗5種 (放置/孤立/重複/ゾンビ/done未検証) + 来歴鮮度2種 (open放置/見直し合図) を共通の適応度関数 (core decay.rs) で検出。閾値は quality.toml の `[decay]` (既定で検知)・古さは ID/note 日付と today の差・next は要約報告・verify.run は data.decay・commit ゲートは保守で構造的腐敗 (done未検証/ゾンビ) を deny・age 系は止めない。詳細 `docs/decisions/20260614-Phase7-腐敗検知の中核.md`・`docs/validation/20260614-Phase7-腐敗検知の中核.md`
- スライス2 = コード/repo 腐敗。owox 直接検出は内容一致の重複ファイル (decay.rs の run_code_decay)、死コード等は `[[decay.checks]]` でプロジェクトの検査コマンドへ委譲 (非ゼロ終了を decay 報告・verify.checks の pass/fail ゲートと区別)。報告は verify.run の data.decay のみ (next は高速維持・commit はコード decay を止めない・advisory)。詳細 `docs/decisions/20260614-Phase7-コードrepo腐敗検知.md`・`docs/validation/20260614-Phase7-コードrepo腐敗検知.md`
- ゴール: 正本が古びず検証つきで育つ
- 作るもの: 検証可能タスク層 (完了に検証必須・腐敗検知)、腐敗検知をコード/repo 全体へ拡大 (庭師の検出側。`docs/decisions/20260613-庭師-剪定.md`)、記憶の鮮度と時間軸、文脈のトークン量の数値表示と秘密情報走査、ブランド固定の機械検証、状態適応、経験の export / import (汎用のみ)
- 人間がすること: タスクを溜めて運用し、古い判断が腐敗検知されるか見る。経験を別プロジェクトへ持ち込む
- 確認方法: 完了タスクに検証があるか / 古い記憶が警告されるか / 文脈量が数値で見えるか / 経験にドメイン固有情報が混ざらないか
- 確認できること: 最小コンテキストが主張でなく測定可能な事実になる
- 完了の目安: 正本が腐らず育つ運用が回る

## Phase 8: 経験を厚くする — 完了・M4 達成

参照: `docs/requirements/20260616-Phase8-経験を厚くする.md`、`docs/decisions/20260616-Phase8-*.md`、`docs/decisions/20260616-canon編集の統一維持.md`、`docs/validation/20260617-Phase8-対話検証と粗の是正.md`

- ゴール: プロジェクトの記憶を厚くし、作業から技を育てる導線を強める
- 作るもの: 調査知識層 (`.owox/knowledge/`・1調査1ファイル・調査日/ソース/要約必須・経過日数で鮮度・オンデマンド読み・supersede 専用・ドメイン分離で経験 export 除外)、スキルテスト是正 (契約 lint を全スキル共通の最低ゲートに・テスト定義の明確化・純散文は自動起動不可・LLM 評価は Phase10 opt-in)、パターンからスキル育成 (使用履歴を MCP+PreToolUse で記録・頻出手順を助言・育成は既存ライフサイクルへ)、canon 編集の統一維持 (分割せず説明簡潔化)
- 人間がすること: 調査を蓄積し古い記憶が腐敗検知されるか見る。繰り返す手順が提案されスキルへ育つか確かめる
- 確認方法: 調査が tool で書け引け古さが警告されるか / 経験 export に調査記憶が混ざらないか / 契約 lint で壊れたスキルが弾かれるか / 頻出手順が表面化するか
- 確認できること: 経験層が厚くなり、記憶と技がプロジェクトに積み上がる
- 完了の目安: 調査記憶が腐らず育ち、手順がスキルへ育つ導線が回る
- 結果: 4スライス実装・cargo 245 passed・MCP stdio/hook で機械確証、実機 Codex 対話で全観点確証 (調査知識の直読みせぬ往復・古い調査の stale 警告・契約 lint で壊れたスキルが draft・canon.propose の人間ゲート・canon.add 未知 section フィードバック)。検証中の粗2件を是正 (床 routing に knowledge 行・routine の distinct フィルタ)

## Phase 9: 規模・移植・方向付け

参照: `docs/requirements/20260611-core-要件.md` の C群、`docs/decisions/20260611-オーケストレーション.md`、`docs/decisions/20260611-方向付け.md`、`docs/requirements/20260618-Phase9-性質軸適応とブランチ記憶.md`、`docs/decisions/20260618-Phase9-*.md`

- 進行中。性質軸適応機構とブランチ作業記憶層を実装 (5スライス・cargo 285 passed・clippy 0・実機検証は未)。AI 駆動開発の方法論 (PRFAQ・理想先行・段階化・クリーンアーキ) を性質で出し入れ: profile.toml と軸解決 (4軸2値・束→active モジュール・既定フル)、層×phase のゲート合成 (別種 AND・max・guarded 操作前ゲート)、優先度/layer/stage 属性と軸ゲートの next 報告、kickoff 束導線・逆生成 (profile.detect)・req 起草コマンド、ブランチ作業記憶層 (stale-branch-memory)。詳細 `docs/decisions/20260618-Phase9-性質軸適応機構.md`・`20260618-Phase9-ブランチ作業記憶層.md`、要件 `docs/requirements/20260618-Phase9-性質軸適応とブランチ記憶.md`
- オーケストレーション (実装済・実機で写像方向を確定): 正本 agents.toml (役割5×変種+責務+ティア+sandbox+許可層・`crates/core/src/agents.rs`)・config.toml の tier→(model, reasoning_effort) 写像・床の `## Orchestration` 能動提示・subagent 権限は可逆実作業に限定し核は触らせない。Codex 写像は実機で custom agent_type spawn 不可と判明 (組込み default/explorer/worker のみ) → 設計の安全弁どおりフォールバック (組込み型+spawn 時 prompt/developer_instructions 注入・床が駆動) を採用し `.codex/agents/*.toml` 生成は撤去。実機 `docs/validation/20260620-Phase9-オーケストレーション実機.md`・要件 `docs/requirements/20260620-Phase9-自律度とオーケストレーション.md`・採用判断 `docs/decisions/20260620-オーケストレーション具体化.md`
- 承認と自動改善 (点1・点2 実装済): 点1=gate.approve 詰みの文言是正は完了。点2=自動承認パスを再設計どおり実装 (既定は architecture 軸導出 flat オン/layered オフ=`crates/core/src/profile.rs` auto_approval_default・同意源 profile 永続/session 限りの2系統=`crates/mcp/src/serve.rs` AutoApproval・対象は層×不可逆の積・後追いキューは来歴一本)。残は点3 訂正検知→practice 草案自動起草。詳細 `docs/decisions/20260620-自律度根本方針と自動承認パス再設計.md`・`20260619-承認と自動改善ループ.md` (点3/点4/集約は有効)
- 自律度の根本方針 (実装済): 最初を厚く握りあとは任せる+3安全弁 (外部副作用=独立最優先ガード=既存 hook pre_tool_use 再利用 / 核判断の湧出=二段ゲートで物質化トリガ限定 / 時間コスト=最初を厚く+後追い早期)。新層は作らず既存自律度勾配の閾値調整。詳細 `docs/decisions/20260620-自律度根本方針と自動承認パス再設計.md`
- MCP コンテキスト削減 (実装済): tools/list の固定占有を生成後除去 (B 方式 owox 完結・`crates/mcp/src/serve.rs` clean_schemas で title/null・空配列 default を全 route 除去・$schema は実機受理確認まで保持)+冗長除去+description 短縮で削減。実機 Codex が cleaned schema を受理。詳細 `docs/decisions/20260620-MCPコンテキスト削減.md`
- マルチ CLI 生成 (実装済): Claude Code 第2対象・OpenCode 第3対象 (Gemini から人間判断で変更)。詳細 `docs/decisions/20260621-Phase9-マルチCLI生成.md`
- 既存コードからの正本逆生成 (実装済): canon.detect で rules/quality の初期案。詳細 `docs/decisions/20260621-Phase9-canon逆生成.md`
- 経験層スケール・GitHub 連携・kickoff 束ね (実装済): 人間4要望へ対応。床注入の件数閾値で自動降格 (practices は新しい順上位 + practice.lookup・glossary は一覧を出さず glossary.lookup へ)、glossary 別名 (`用語 | 別名: 定義`・上限5)、task の GitHub Issues 双方向 (core 非ネットワークのまま入口 skill issues + task external ref で対応付け)、kickoff MCP tool 束ね (向き付け+性質+手薄なら逆生成案を1呼び出し・後入れ導線)。cargo 372・clippy 0。詳細 `docs/decisions/20260621-Phase9-経験層スケールとGitHub連携とkickoff束ね.md`
- 残 (実機/対話検証・人間関与): 性質軸とブランチ記憶の実機/target 検証・オーケストレーションと自動承認の対話 Codex 検証 (フォールバック実効・subagent 内 PreToolUse・auto 承認の MCP 完走)・マルチ CLI の実機 (Claude Code の hook 発火と床注入・不可逆 deny の throw)・canon.detect の逆生成精度
- ゴール: 複数 CLI・既存プロジェクト・チームへ広げる
- 作るもの: マルチ CLI 生成 (Claude Code。`docs/decisions/20260621-Phase9-マルチCLI生成.md`)、オーケストレーション (役割別 subagent・権限・モデル振り分け。多視点+敵対レビューの subagent、庭師 subagent/掃除 skill = 剪定の実行側)、既存コードからの正本逆生成、kickoff (立ち上げ・Vision・次の一手・判断支援)
- 人間がすること: 別の CLI で同じ target repo を使う。既存プロジェクトに後から入れる
- 確認方法: 別 CLI でも方針と境界が保たれるか / 既存 repo から正本の初期案が出るか
- 確認できること: 特定 AI に閉じない、既存プロジェクトに入れられるという差別化軸
- 完了の目安: 第2対象ユーザへ届く

## Phase 10: 条件付き機能と配布 (製品完成)

参照: `docs/requirements/20260611-core-要件.md` の D群、`docs/decisions/20260611-技術スタック.md`、`docs/decisions/20260611-チーム権限.md`、`docs/decisions/20260611-eval方針.md`、`docs/decisions/20260621-Phase10-配布とrelease正本.md`、`docs/requirements/20260621-Phase10-配布と条件付き機能.md`

- 配布スライス着手・実装 (2026-06-21)。owox-harness 自身の配布 (release workflow 4ビルド・SHA256SUMS・setup.sh/install.ps1・版 0.1.0・owox --version) と release.toml (任意正本・release.check MCP tool) を実装。cargo/clippy/fmt/shellcheck で機械確証。残は実 tag push の workflow 実走・別環境導入・release.check の dist 照合 (人間関与)。eval とチーム権限は未着手
- ゴール: opt-in 機能を足し、他者が導入できる製品にする
- 作るもの: eval (改善検証・opt-in)、チーム権限 (役割・承認委譲)、release.toml、配布 (GitHub Releases + setup.sh + checksum 検証)、人間向け文書
- 人間がすること: 配布物をダウンロードし導入してみる。チーム運用を試す
- 確認方法: checksum 検証つきで導入できるか / 承認が来歴に残るか
- 確認できること: 製品として配れる
- 完了の目安: 製品完成

## マイルストーン

- M1 (Phase 5 末): 第1段階完成。Codex で一通り回る — 達成 (2026-06-13・実機 Codex 対話で確証)
- M2 (Phase 6 末): コア完成。旗が立つ — 達成 (2026-06-14・cargo / MCP stdio の機械確証 + 実機 Codex 対話検証、粗2件是正済み)
- M3 (Phase 7 末): 腐らず成長が動く — 達成 (2026-06-16・cargo 217 passed / MCP stdio・hook の機械確証 + 実機 Codex 対話検証。コンテキスト配信再設計と PostCompact→SessionStart compact 是正を含む)
- M4 (Phase 8 末): 経験が厚く育つ — 達成 (2026-06-17・cargo 245 passed / MCP stdio・hook の機械確証 + 実機 Codex 対話検証。調査知識層・契約 lint・パターンからスキル育成・canon 統一維持。粗2件是正: 床 routing に knowledge・routine の distinct フィルタ)
- M5 (Phase 9 末): マルチ CLI・チーム土台 — 実装一巡 (cargo 369 passed・clippy 0)。マルチ CLI 生成 (Codex/Claude Code)・canon 逆生成まで揃う。達成判定は実機/対話検証 (人間関与) の後
- M6 (Phase 10 末): 製品完成。配布可能

## 人間の関与が重い場所

- Phase 1: 実機検証 (推測の確証)
- Phase 2: 正本の項目構造を決める (体験の根幹)
- Phase 3: 何を不可逆 (確認必須) とするか
- Phase 6: 要件と受け入れ基準を書く

他フェーズは owox 開発側が主導し、人間は要所のレビューと承認に集中できる。
