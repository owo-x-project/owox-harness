# decisions

採用判断、技術選定、見直し条件を置く。

## 置くもの

- 採用した案
- 採用理由
- 捨てた案
- 捨てた理由
- 見直し条件

## 読む時

- 設計に迷う時
- 技術選定の理由を確認する時
- 既存判断を変える時

## 文書

- `20260611-全体像.md`: 俯瞰の地図 (.owox/ 構造・流れ・各決定の関係)。迷ったらまずここ
- `20260611-製品戦略.md`: 実現アプローチ、中心の旗、差別化軸
- `20260611-技術スタック.md`: 実装言語、正本形式、配布、生成、検証、入口
- `20260612-正本フォーマット.md`: prose 正本=Markdown・英語固定 / machine 設定 (config.toml。targets 等)=TOML。型は読込後に検証
- `20260612-段階的開示.md`: 最小コンテキストの仕組み (常時=気づき / 関連時 / オンデマンドの3層。用語名と定義の分離、ルールのスコープ)
- `20260611-開発アーキテクチャ.md`: crate 構成、Target 変換、データ配置、経験分離
- `20260611-第1対象CLI.md`: 第1対象は Codex CLI (成立根拠、制約と回避策、2026-06 調査)
- `20260611-設計原則.md`: メモリ階層、マルチエージェント、可逆性境界、適応度関数、来歴、回帰防止、スキル・ライフサイクル、ブランド固定
- `20260611-target-harness内容.md`: 正本ソース .owox/、生成物、MCP 操作、hook 実体、実装順
- `20260611-MCP設計.md`: resource と tool、共通返り値、人間ゲート、経験の入出力
- `20260611-制御方針.md`: AI の MCP 利用制御 (機械強制 / 指示 / 誘導、状態適応、トリガ)
- `20260612-Phase3-hook実装.md`: hook 入出力契約 (一次情報)、安全核を縦貫、不可逆=owox 既定+target 追記、薄いシェル廃止、human_gate との関係
- `20260613-Phase4-MCP実装.md`: MCP サーバは公式 SDK rmcp 採用 (自作却下)、tokio は time のみ、Codex は resource を read_mcp_resource ツールで開示 (一次情報・実機確証)
- `20260613-Phase4-tool記録層.md`: 記録層 (1 件 1 ファイル Markdown・ID=日付+slug)、gate=status=open の来歴、gate (永続) と needs_human (一時) の違い、来歴の鮮度・退避、封筒は core の型 (schemars 無し)、verify は検証完了だけ機械判定・検査は config.toml 明示、日付は mcp が与える
- `20260613-庭師-剪定.md`: 農家 (育てる) と対の庭師 (刈り込む) を一級概念に。腐敗検知をコード/repo 全体へ拡大、庭師 subagent/掃除 skill を生成、剪定は verify+削除基準を必ず通す (盲目削除しない)
- `20260611-タスク管理.md`: 検証可能タスク層 (Beads 超え。done 検証必須 / 来歴連動 / 腐敗検知 / 状態適応)
- `20260611-方向付け.md`: 迷わない仕組み (kickoff skill、Vision、次の一手、未決棚卸し、判断支援)
- `20260611-品質保証.md`: 品質・堅牢性 (plan-alignment、要件↔テストtrace+受け入れ基準、適応度関数、回帰捕捉、多視点レビュー)
- `20260611-オーケストレーション.md`: subagent パターン生成、役割→権限、モデルのティア振り分け、handoff
- `20260611-ショートカット.md`: 人間の slash command (正本→生成、namespace+別名、プロジェクト拡張可)。Codex は skills へ寄せる (Phase5 で転換)
- `20260613-Phase5-スキルと入口.md`: Phase5 設計。Codex native Skills (.agents/skills/) へ寄せる、正本分離・生成、テスト合格ゲート、昇格3状態 (昇格で implicit 解禁)、経験メモリ更新 (skill.remember)、入口=薄い skill (明示起動)、カタログ=tool、glossary 動的化 (CRUD tool・add可/update・removeは人間ゲート・用語名は session 注入)、canon 直読み禁止 (指針 canon 対象・skills は例外)、setup は設定生成+検査 (install は Phase9)
- `20260613-Phase5-実機検証の是正.md`: 実機対話で見つけた粗の是正。読み (next/context) を resource から tool へ一本化 (Phase4「読みは resource」を転換)、入口 skill 本文を 1 tool へ命令形で強束ね、openai.yaml を正式スキーマ (policy.allow_implicit_invocation + interface) へ、応答言語を正本設定+SessionStart 注入で追従
- `20260614-Phase6-requirements正本層.md`: Phase6 スライス1 設計。`.owox/requirements/` に1要件1ファイル (記録層踏襲)、状態はライフサイクルのみ (draft/accepted/superseded)・充足は保存せず導出、受け入れ基準はエントリ+属性方式 (irreversible 流用)・基準ごと検証 link・id は永続番号、tool 6個 (create/list/get/update/add_criterion/link_verification)、title/statement 変更のみ来歴連動
- `20260614-Phase6-レビュー枠組み.md`: Phase6 スライス4 設計 (routable)。観点 (correctness/design/security/plan-alignment/requirement/pruning) は owox 標準・具体バーと追加はプロジェクト。観点を離散単位にし適用トリガ (Always か Paths glob) で変更内容から機械選択 (review.lenses tool・select_lenses 契約・依存マニフェスト変更で plan-alignment を既定強化)。進め方は機械検証済み取り込み→多視点→事実性確認→敵対的再確認、pruning は庭師の安全規則。枠組みは実行モデル非依存で Phase8 のルータ (subagent 並列・ティア振り分け) が同契約を消費。review.toml で per-project トリガ拡張
- `20260614-Phase6-quality適応度関数.md`: Phase6 スライス3 設計。quality.toml (別ファイル machine 設定) に owox 直接検証2種 (ファイル行数予算・禁止パターンで層境界/依存方向) を宣言、循環/複雑度は検査コマンド委譲、glob は regex 変換・ファイル列挙は git ls-files を mcp が渡す、効かせ方は commit で phase 適応 (保守 block・初期/安定 警告)・verify.run は常時報告、判定は commit に集約
- `20260614-Phase6-要件完了の機械判定.md`: Phase6 スライス2 設計。検証 link は config 検査名を指し書き込み時照合で dangling を作らせない (task.create と同型・spec-first は unlinked が中間状態)、要件完了は accepted ごとに met/needs_human/failed を判定、completion.requirement を met へ移し旗を立てる、trace 欠落は next で先回り+verify.run で確定、commit ゲートは変えない
- `20260614-Phase6-対話検証の是正.md`: Phase6 対話検証の継続観察3件の是正。来歴 ID は対象 ID の日付プレフィックスを剥がし slug を 40 字上限で語境界切り (一意性は連番が担保・本文は full)、Stop は作業ツリー署名で「変更が変わった時だけ促す」(session キャッシュで hook 完結・dirty_changed を core へ渡す)、link_verification の照合 (config 検査名・未知は弾く) をツール説明へ明記。検査名一覧 tool は増やさない
- `20260614-Phase7-腐敗検知の中核.md`: Phase7 スライス1 設計。core decay.rs (新規) に run_decay 純関数 (タスク+来歴+閾値+today→違反並び)、閾値は quality.toml の `[decay]` 節 (既定で検知・数値は phase で変えず enforcement だけ phase 適応)、古さは ID 日付と note 日付の最新と today の差 (git mtime 不使用・civil 変換は core 純関数)、検出はタスク5種 (放置/孤立/重複/ゾンビ/done未検証・orphan は age ゲート・重複は slug 完全一致) + 来歴2種 (open 放置・見直し合図)、報告は next の腐敗節 (要約) と verify.run の data.decay (報告のみ)、commit は構造的腐敗 (done未検証/ゾンビ) だけ保守 block・age 系は助言、today_utc を mcp clock.rs へ切り出し serve/hook 共用
- `20260614-Phase7-コードrepo腐敗検知.md`: Phase7 スライス2 設計。庭師の検出側をコード/repo へ拡大。owox 直接検出は内容一致の重複ファイル (decay.rs の run_code_decay・内容でグループ化し衝突なし・min_duplicate_bytes 未満は無視)、死コード等は `[[decay.checks]]` 新設でプロジェクトの検査コマンドへ委譲し owox が走らせ非ゼロ終了を decay-check として報告 (run_check 再利用・verify.checks の pass/fail ゲートと区別・advisory)。DecayConfig へ min_duplicate_bytes と checks (model::VerifyCheck 再利用) を追加。報告は verify.run の data.decay のみ (next は高速維持で repo decay 非搭載・commit はコード decay を止めず構造的タスク腐敗のみ block)。run_verify 署名は不変
- `20260614-Phase7-測定可視化とブランド検証.md`: Phase7 スライス3 設計。トークン量可視化は core tokens.rs の estimate_tokens (CJK 1 文字 ≒ 1 トークン・他 4 文字 ≒ 1 トークンの軽い推定・新規依存なし・pass/fail ゲートにしない)、context tool の描画末尾へ表示。秘密走査は core secret.rs (irreversible 方式の組込み正規表現・値は漏らさない) を context 出力へ・警告のみで block しない (スライス4 の export/import が本命再利用)。ブランド検証は禁止語を glossary.md の予約見出し Forbidden に正規表現で構造化 (用語集が用語+定義+禁止語の正本・読込時検証)、run_brand が QualityViolation kind="brand" として既存 quality チャネルへ報告 (commit ゲートの phase 適応も共通)、正規表現で表せる表記規則は既存 quality.toml boundaries を流用 (新機構なし)・助詞省略など構文要は対象外。run_verify 署名は不変
- `20260614-Phase7-経験IOと二層ルール.md`: Phase7 スライス4 設計 (人間判断で二層ルール統合)。経験 export/import は core experience.rs (経験束 = skill の SKILL.md+scripts + 成長層 practices.md・memory/tests/owox.toml/固定層は型で除外・本文は secret::scan・export はクリーンなら書出+needs_human/秘密は failed・import は秘密で needs_human/クリーンは draft 書き+practices マージ)。二層ルール = 固定層 (brand/rules/glossary・人間が握る) と成長層 (practices.md・AI が practice.add で育てる・鮮度 decay run_practice_decay の stale-practice・session_start ライブ注入)。canon.propose (core proposal.rs) で AI が固定層変更を提案→open gate→人間 (glossary.update と同じ型・欠落を埋める)。状態適応は session_start_context の phase 案内が state.toml 永続で再起動後も効く回帰テスト。run_decay 署名は不変 (practice decay は別関数)。追補 (統一 canon 編集): 人間判断で brand/rules/practices/glossary を 1 モデルへ統一 — add は AI 直接+来歴・change/remove は canon.propose で人間ゲート。MCP は canon.add/canon.propose/glossary.lookup の 3 つへ集約 (glossary.add/update/remove・practice.add・旧 canon.propose を撤去・core は canon.rs へ畳む)。二層は権限差でなく内容差
- `20260616-Phase7-コンテキスト配信の再設計.md`: Phase7 対話検証で残った 3 懸念 + 曖昧リクエスト対応の再設計。AGENTS.md 廃止し床コンテキスト (向き付け・意図ルーティング・Vision・全体スタイル・状態・用語名・practices) を core floor_context が組み SessionStart で注入。圧縮・再開後の床復元は SessionStart の matcher を startup|resume|compact へ広げて担う (当初 PostCompact 案は実機で additionalContext 非対応と判明し切替)。意図ルーティング表を床に常時置きユーザーが tool 名/用語を言わなくても要望から tool へ飛べるように (canon.propose/next/context/glossary.lookup/rules.lookup/review.lenses)。rules 本文を `## Rules` へ集約 (`###` 小節)・render_rules_block を lookup と語トリガで共用。rules/brand は 3 経路 (UserPromptSubmit 語トリガ policy_injection・rules.lookup ツール・PreToolUse 編集直前の force_rules push)・brand は values/principles/non-goals/success-criteria のみ語トリガで Vision と全体スタイルは床常時・重複排除は session キャッシュへ policy:rules/policy:brand キーで載せ用語名と非衝突。捨てた案: 最小 AGENTS.md を安定床に残す (ユーザ判断で単一経路優先)・brand.lookup・全体スタイルの on-demand 化。見直し: PostCompact が実機で床を戻さない時
- `20260616-Phase8-調査知識層.md`: Phase8 設計。`.owox/knowledge/` に1調査1ファイル (記録層踏襲)、型は調査日/ソース/要約/tags/status/supersedes、tool 4個 (add/list/get/lookup・更新は supersede 専用)、鮮度は decay.rs の run_knowledge_decay (経過日数 > knowledge_stale_days で stale-knowledge・advisory)、読みはオンデマンド (注入しない)、add で secret 走査、ドメイン固有なので experience.export から除外。捨てた案: practices 拡張・in-place 更新・常時注入
- `20260616-Phase8-スキルテスト是正.md`: Phase8 設計。契約 lint (frontmatter・SKILL.md の scripts 参照実在・tests 実行ビット・owox.toml 妥当) を全スキル共通の最低ゲートへ (registrable_shape 拡張・owox 自動で人間が書かない)、テスト定義を「依存する script/tool の振る舞いを確かめる」と明確化、決定的にテストできない純散文は自動起動不可を明確化、LLM 振る舞い評価は Phase10 opt-in・助言・ゲートにしない。捨てた案: LLM 評価をゲートに・散文中の tool 名 lint・空テスト検出ヒューリスティック
- `20260616-Phase8-パターンからスキル育成.md`: Phase8 設計。使用履歴 `.owox/usage.log` (追記専用・名前と日付のみ・引数残さず) を MCP serve と PreToolUse hook の両方で記録、core routine.rs の run_routine_suggestions が隣接手順 (長さ2..=4) の頻出 (`[routine]` 閾値) かつ未スキル化を助言、報告は next / verify.run (advisory・止めない)、育成は既存ライフサイクル (テスト合格+人間昇格) へ流す。雛形自動生成はしない。捨てた案: 雛形自動生成・引数記録・PostToolUse 追加・MCP のみ記録
- `20260617-practices冗長性の機械シグナル.md`: Phase8 実機検証で見つけた、似た practice が無検査で溜まる問題の対処。practices.add に完全一致ガード (同一テキストは別日でも足さず来歴も増やさない)、decay.rs の run_practice_redundancy が practice 対を字 n-gram (3-gram) Jaccard で比べ閾値 (`[decay]` practice_similarity 既定0.5) 超えを redundant-practice として next/verify.run へ advisory (統合は canon.propose へ)。字 n-gram は言語非依存・決定論 (LLM 不要)。AI の気づき任せでなく機械検出 + 床注入の膨らみ (最小コンテキスト旗) を守る
- `20260616-canon編集の統一維持.md`: canon.add / canon.propose の統一を維持し型別分割しない判断の明文化。編集セマンティクスが全 canon 種で同一なので型で割ると同一振る舞いの tool が並び逆に迷う、tool は振る舞いで割る (読みの glossary.lookup/rules.lookup は形が違うので分割が正しい)、実機で確証済み。是正は分割でなく説明簡潔化と不一致フィードバック
- `20260616-Phase7-canon変更の自動適用と承認の機械強制.md`: Phase7 対話検証で見つけた粗の是正。canon.propose に構造化変更 (op=remove/replace・item を見出し配下で照合・不一致は現項目を data.items で開示し再提案させる・自由文は従来どおり手編集の逃げ道) を足し、gate.approve が来歴の `## Proposed change` を canon へ適用 (apply_pending_canon_change を承認前に実行・失敗なら承認しない・propose 後に canon が変われば適用せず open 維持)。gate.approve へ destructive_hint 注釈で Codex が承認モード問わず必ず人間確認。rules 内容を複製した編集可能な AGENTS.md を AI が直接編集する粗のため、brand リスト + rules を AGENTS.md から外し SessionStart 注入へ移し Vision のみアンカーに残す (Phase5 の glossary live 注入方針の延長)。canon 直読み deny の owox:// 陳腐化も是正。ProposedChange・record_decision_with_change・load_decision を追加・承認の合成は serve.rs (循環依存回避)
- `20260611-チーム権限.md`: 役割ベース権限、正本変更は CODEOWNERS/PR 委譲、承認 identity を来歴に
- `20260611-eval方針.md`: 検証つき成長の測定層 (決定論優先、改善検証先、golden-set/paired-delta、opt-in)
- `20260613-Vision語彙とセッション目標.md`: 永続目的=Vision に語彙統一、セッション目標は持たない (人間プロンプト直挿し)、次の一手 (owox://next) と区別
- `20260618-Phase9-性質軸適応機構.md`: Phase9 設計 (採用判断・未実装)。AI 駆動開発の方法論 (PRFAQ・理想先行・段階化・クリーンアーキ) を性質で出し入れ。phase=時間軸と直交する性質軸を追加 (再変更可)、軸が基本単位・プリセットは砂糖衣 (4軸 requirements-shape/prioritization/delivery/architecture)、既定束=フル方法論・軸で上書き可・全 off 可、宣言は新規 profile.toml (固定) で state.toml (可変) と分離、4方法論は既存面へ (要件起草スキル req 単一・優先度属性=人間ランク・stage タグ・quality.toml 層境界を唯一真実に3分解)、層別自律度勾配、軸×phase 合成は別種ゲート AND・畳む時 max・はしご1本 (free<warn<commit-block<human-gate)、guarded 発火 v1=4段 (境界違反+削除+契約面パス宣言+他は助言)、逆生成は厚いシグナルで寄り推定し draft+根拠を人間確定、kickoff は束を人語で選び生軸を触らせず、profile.toml 型 (preset+axes 上書き+独自束・軸レジストリ/標準束は組込・値は列挙 v1=2値・未知は弾く・根拠非保存)。捨てた案=固定プリセット列挙・投機的軸追加・永久固定
- `20260619-承認と自動改善ループ.md`: 人間4発言の確定。点1=gate.approve 詰みの是正 (注入文 約7か所を「人間が承認と判断したら AI が gate.approve を呼ぶ・CLI 確認が人間ゲート・人間に実行依頼しない」へ統一・挙動不変・実装済)、点2=自動承認は既存自律度勾配で線引き (guarded=常に人間・supervised/free=owox 所有 auto パス destructive_hint 無で `approval:auto` 印+後追いキュー・auto はオフ既定で人間が CLI 確認付きオン・セッション限り)、点3=新語入れず practices/skills の能動提示を kickoff 束導線へ相乗り、点4=訂正検知 (決定論コア=AI 直後出力の手編集 + AI 自己申告) → practice 草案自動起草 → 後追いキュー → 人間承認で固定。点2 と点4 は同じ後追い確認面に集約。捨てた案=全ゲート auto・可逆性で新軸・auto なし・製法独立機構・完全自動ループ・auto 永続・差分のみ検知。機構の壁=destructive_hint は静的注釈で auto は gate.approve を通せず別パス必須
- `20260619-Phase9-guarded承認で解凍.md`: Phase9 実機検証で見つけた guarded 層ゲート2粗の是正。粗1=層ゲートが apply_patch のパッチ本文を解析せず実機 Codex で空振り (Codex は削除・編集を全て apply_patch で送りパスを tool_input.command の絶対パスに置く) → parse_patch_changes でパッチ封筒 (Add/Update/Delete File・Move to) からパス抽出し work_dir 基準で repo 相対化して層 glob 照合 (Bash rm・Edit/Write file_path・apply_patch を一様化)。粗2=guarded を deny した後に承認しても適用器が無く詰む (canon は apply_pending_canon_change で自動適用だがコードに無い) → 来歴に authorizes (承認時解凍する具体パス) と consumed (one-shot 印) を足し proposed_change と対称化、AI は decision.record の authorizes で触りたいパスを宣言し open ゲート、人間 gate.approve で adopted、層ゲートは guarded 対象が adopted・未消費の authorizes に全て覆われれば 1 回 Thaw し consumed に・1 つでも欠ければ deny、再操作は再ゲート。gate_autonomy が authorizes 持ちを常に Guarded と判定し gate.auto_approve が自動拒否する二重ガード (自己承認で核を解凍させない)。来歴を単一真実源にし cache へ解凍トークンを書く案は却下 (二重管理・寿命曖昧)。責務は core 純ゲートが Allow/Deny/Thaw を返し状態 (収集・消費) は mcp。cargo 301・clippy 0・hook/MCP 経路確認済・実機 end-to-end は検証中。見直し=authorizes 誤記頻度・操作種での絞り要否・apply_patch bulk 削除が不可逆検出を素通りする隙
- `20260618-Phase9-ブランチ作業記憶層.md`: Phase9 設計 (採用判断・未実装)。性質軸機構と独立。ブランチ単位の作業記憶で並行作業 (worktree・マルチエージェント) の文脈衝突を解消。work/ を拡張しブランチ別キー・git-ignored 継続 (新層足さず・保管場所を正規化し worktree 横断で同じバケツ)、キー=ブランチ名で rename/rebase 自動追従なし、ライフサイクル=自動削除せず stale 検出で剪定・昇格は明示 (decisions/experience へ)、並行作業はキーで自然分離 (同一ブランチのマルチエージェントは共有)、性質軸とは v1 疎結合、共有は同 repo 同ディスクのみ (マシン跨ぎは対象外)、床非注入のオンデマンド読み・書込時 secret 走査・stale-branch-memory 腐敗検知。捨てた案=別層新設・git-track でブランチに載せる・自動破棄・rename 自動追従
- `20260620-Phase9-人間ゲートの確認依存.md`: Phase9 実機検証で確定した trusted の抜け穴への採用判断。guarded ゲートの人間同意は destructive 注釈のクライアント確認だけに依存し、Phase7 の「上書き不可」前提に trust_level=trusted という抜け穴があった (trusted で確認が省かれ AI が rules 等を自己承認可)。判断=owox が生成する target 設定でゲートツール (gate.approve・gate.auto_enable・canon 書き戻し) だけを全面信頼下でも常時確認へ固定し、人間ゲート保証は「クライアントが確認を出すこと」が前提と明文化。同意信号は AI の外からしか来ないので、AI が触れない設定生成面で前提を固定する。技術裏付け=Codex 0.141.0 の RawMcpServerConfig が per-server (default_tools_approval_mode) と per-tool の承認設定を持つ (モード列挙 UnlessTrusted/OnFailure/OnRequest/Never)。未確定 (実機要確認)=trusted でも必ず聞くモード値 (候補 OnRequest)・正確なキー構造・プロジェクト設定がグローバル trust に勝つか。捨てた案=owox 独自の腕外承認経路 (摩擦復活・承認面2つ)・明記のみ (保証が黙って蒸発)。実装は次スライス (render_mcp_config へ承認固定・マルチ CLI へ写像・生成テスト・実機モード値確定)
- `20260620-MCPコンテキスト削減.md`: tools/list の固定占有を実測し直し削減を確定。実測訂正=handoff 概算「inputSchema 24,560字・doc comment 転写9,100が本命」は誤り、実 inputSchema 16,073字/5,114tok・description 転写 3,688字/1,997tok・ツール46個、最大費目は doc comment でなく構造定型 ($schema 反復34回1,904字+struct title 946字+default:null/[]+nullable ,null = 約850tok)。採用=構造定型除去 (B 生成後除去で owox 完結・rmcp 改造不要・ToolRouter.map/Tool.input_schema は pub・Arc::make_mut で COW・tool 追加に自動追従・title/default は完全安全で無条件・$schema だけ実機 Codex 検証ゲートで欠落拒否なら残す安全弁)+inputSchema 冗長除去 (`(任意)` 等括弧補足・約280tok・括弧禁止方針と整合)+本体 description 30%短縮 (約1,000tok・手作業・発見性維持で重複冗長のみ)。合計約2,130tok減 (起動時 MCP 実載 約8,500の約25%)。捨てた案=enum 化 (差し引き±0tok・品質目的なら可)・遅延公開 (発見性=床 routing から外れ AI が存在を知れず・動的公開機構無し静的フィルタは恒久無効化・820tok にリスク見合わず)・A 生成時抑制 (rmcp が SchemaSettings 内部固定で差し込み不可)。見直し=Codex が $schema 欠落拒否・短縮で選択精度低下・rmcp/schemars 更新で生成経路変化
- `20260620-自律度根本方針と自動承認パス再設計.md`: `20260619-承認と自動改善ループ.md` の点2 を supersede。根本方針=最初を厚く握りあとは任せる+3安全弁 (穴A 外部副作用=不可逆を層・auto と直交させ独立最優先ガード・auto 絶対迂回せず / 穴B 核判断の湧出=二段ゲートだが物質化トリガ限定で guarded 資産への操作だけ機械検知・止まれは散文不可・純設計判断は可逆なので委ね後追いレビュー / 穴C 時間コスト=最初を厚く+後追い早期で手戻り予防)。新層は作らず既存自律度勾配の閾値調整。点2 再設計=auto 既定を architecture 軸から導出 (layered オフ/flat オン・新軸足さず・組込5束で auto 適性と完全相関・preset 無しは慎重側オフ)、同意源と寿命2系統 (profile 由来=人間の宣言が同意源で永続・session 由来=明示オンで session 限り切り忘れ防止)、対象集合=層 (supervised/free)×不可逆 (除外) の積で機械決定し許可リスト無し、後追いキュー=来歴 (Decision) 一本に `approval:auto` 印+未確認状態 (cache/専用ファイルは却下・単一真実源)。捨てた案=同等権限の新層・一律オン/オフ・autonomy 5軸目・preset ごと auto 定義・寿命一律・明示許可リスト・純設計判断の自己申告ゲート・着手前チェックポイント。見直し=flat で慎重/layered で auto の実需 (autonomy 軸へ昇格)・手戻り頻発 (着手前確認再検討)・永続が広すぎ・不可逆が auto を通る事故
- `20260620-要件分類とPRFAQ正本.md`: `20260618-Phase9-性質軸適応機構.md` の未定義2点を決着。正本は要件正本層 (`.owox/requirements/`) の一本で、PRFAQ も lightweight も保存先を変えない (PRFAQ は起草層・出力を要件へ蒸留・プレスリリース文書は別に残さず二重正本を断つ・便益となぜは来歴へ link)。要件へ任意の種類タグ (functional / non-functional の2値) を追加し、性質軸と独立でどの要件にも付けられる (機能=テスト link で met 判定向き・非機能=quality.toml 連動側)。技術・設計の制約は要件でなく来歴へ (二重管理回避)。種類で対象者は分けない・正本は機械可読で AI 技術者向け。実装=core RequirementKind と `## Kind` 節・MCP の kind 引数・req スキル本文の蒸留と種類タグ付け。捨てた案=種類タグ無し・3値/4値・PRFAQ 文書併存・lightweight 一本化。実機 Codex で req 検証したところ方法論が全く適用されず (床ルーティングが要件要望を req スキルでなく requirement.create へ直接誘導し本文が読み込まれない) → 是正3点: 床行を req スキルへ向け非交渉事項を併記・ツール説明へ同事項を移す・ideal-first で AI の create priority を機械ガード (ai_priority_blocked・限界は create のみ・完全な人間ゲートは将来)。cargo 318・clippy 0
- `20260620-オーケストレーション具体化.md`: `20260611-オーケストレーション.md` を第1対象 Codex の subagent 実態調査 (2026-06-20) で具体化。Codex 実態=1エージェント1ファイル TOML (`.codex/agents/*.toml`)・自動委任なし明示 spawn・ファイル名指定 spawn 不可 (agent_type+prompt 上書きのみ)・per-subagent model+model_reasoning_effort 可・subagent 内も PreToolUse 発火・独立コンテキストで親へ summary・ツール許可リスト無し (sandbox_mode+mcp_servers のみ)・max_threads6/max_depth1。4観点採用=担当 (親1階層・owox は非実行でパターン能動提示・適用は親判断)、エージェントタイプ (役割×変種の2次元=基本5役割 調査/計画/実装/レビュー/検証 固定+全役割共通の変種軸・レビュー変種は観点 review.toml 再利用・庭師=pruning 変種・敵対=refute 変種・将来は変種追加で役割固定・Codex 写像はカスタム TOML 主体で役割→TOML/変種→spawn 時 prompt 上書き)、呼び出しルール (スケール適応=task サイズ+state 連動を能動提示し親判断・並列=読取 fan-out/書込逐次・handoff=既存 .owox/ 層へ一時 work/昇格 task 来歴 knowledge verification・summary は通知)、モデル割り当て (4ティア維持で effort は config.toml [targets.codex] のティア→(model,effort) 写像で吸収・役割既定は調査 fast/実装 balanced/計画レビュー検証 strong/親 reasoning・変種上書き 敵対 reasoning)。捨てた案=多階層・専門役割増設・役割フラット展開・prompt 上書き主体・実装並列・findings 新層・summary 主体 handoff・ティア2軸再設計・ティアの profile 連動・スケール機械強制。要実機確認=カスタム agent_type spawn 可否 (不可なら prompt 上書きへ)・subagent 内 PreToolUse 発火・trusted/apps 影響・worktree 分離
- `20260621-Phase10-配布とrelease正本.md`: Phase10 配布の採用判断。owox-harness 自身の配布 = モノレポ root の `.github/workflows/release.yml`・main の `control/.owox-version` 変更起点・値は `owox-v<version>` 形式で Release tag とビルド時 `owox --version` へ追従・native runner 4種 (linux x86_64=`musl` / linux aarch64=`gnu`・macOS aarch64・Windows x86_64) で成果物 (tar.gz/zip)・全成果物の SHA256 を1つの SHA256SUMS へ・setup.sh (unix) と install.ps1 (Windows) が取得→checksum 照合→配置 (既定 `~/.local/bin`)。対象プロジェクト向け release.toml = 任意正本・配布方針 (policy・案内のみ)/版 (`[version]` file+pattern で取り出す・行頭は `(?m)`)/成果物検証 (`[[artifacts]]` 存在確認 + `[[checks]]` 実検証は委譲)・release.check MCP tool で版抽出+成果物存在+委譲検査を封筒で返す。捨=hash の Rust 実装 (新依存)・macOS x86_64/Windows arm・個別 checksum ファイル・release.toml 必須化・署名。見直し=linux aarch64 の `glibc` 差分報告・署名要求・対象拡大・委譲では粗い時の owox 内 hash・control 独立リポジトリ化。実機 (main の `.owox-version` 変更 merge 実走・別環境導入・release.check の dist 照合) は人間関与で未
- `20260621-Phase9-経験層スケールとGitHub連携とkickoff束ね.md`: 人間4要望の採用判断。(1) 経験層スケール = 床注入に件数閾値 (config.toml `[context]`・既定大) を置き超過時だけ降格、practices は新しい順に上位 N 件 + practice.lookup でオンデマンド・glossary 用語名は一覧を出さず glossary.lookup へ寄せる、降格時は床 routing へ lookup を明示し発見性の穴を塞ぐ。捨=即オンデマンド/decay 強化のみ。(2) glossary 別名 = GlossaryEntry へ aliases・正本書式は `用語 | 別名: 定義`・照合と lookup を別名へ広げ床へは正規名のみ・1 用語上限 5 を add で弾く。捨=括弧表記/別名を床へ。(3) GitHub Issues 双方向 = core はネットワーク非保持を維持し gh を呼ぶ入口 skill `issues` で実現・task へ external ref (`github: owner/repo#N`) を足し再同期の重複を防ぐ鍵に・owox が正本で衝突は issue 側を直す。捨=core 連携/片方向。(4) kickoff 束ね = MCP kickoff tool が向き付け+性質 (未宣言なら profile.detect 案)+手薄なら canon.detect 案を 1 呼び出しで返し書かない・後入れ導線を能動化・skill 本文は tool 呼びへ寄せる。捨=現状維持/再生成専用導線。cargo 372・clippy 0
- `20260627-判断2軸と対話kickoff.md`: 判断を製品意図軸 (人間が握る=何を/誰のため/成功条件/リスク/安全境界/優先順位) と行動軸 (owox が握る=tool 選択/verify 可否/skill 化/profile/索引/庭師) に2分割し全 tool・skill の振る舞い基準に。人間 gate は意図正本・安全境界・不可逆操作のみ・行動軸生成物は owox が書いてよい。kickoff は質問を3種 (引き出し=推奨なし可/確認=否認修正だけ/判断=推奨+理由+選択肢) に分け「全質問に推奨必須」を撤回・harness 構成は人間質問でなく owox 決定→確認へ格下げ・人間は自然発話で AI が owox 代理操作。kickoff 外は next が「人間が決める意図」と「AI が今すぐやる行動」を分けて返し行動軸を owox 断定 (検証の指示外 tool/常時 advisory/余分 verify を解消)。`20260611-方向付け.md` の grill-me「各問に推奨案」を意図軸質問に限定する精緻化。捨=判断一語維持/推奨必須維持/harness 構成を人間質問のまま/P 群を対症療法。出典は対話検証 `docs/validation/20260627-要件群対話検証.md`
- `20260620-Phase9-シェル書き込みの層ゲート被覆.md`: オーケストレーション具体化の要実機確認「subagent 内 PreToolUse 発火」を対話 Codex で潰した際の粗の是正。粗=実機 Codex の subagent (worker) は apply_patch を持たず編集を全て exec_command シェルで行い、層ゲートの Bash 経路は rm 削除しか見ず `>>`/tee/sed -i 等の書き込みを検出しないため、subagent の契約面書き込みが guarded ゲートを素通り (親の apply_patch 編集は deny されるのに非対称)。採用=`write_targets` を足しリダイレクト/tee/sed -i/cp・mv 宛先/dd of= を抽出し apply_patch 編集と同じ `is_contract_surface` 照合へ載せる (契約面書き込みは deny・guarded 非契約面と free は素通りで rule4 と対称)。過剰検出は層 glob に当たらず無害なので取りこぼしより過剰側へ倒す近似。捨てた案=shell 完全解析 (保守負債)・不可逆判定側に追加 (書き込みは可逆で層ゲートの責務)・guarded 非契約面も止める (rule4 と非対称化)。見直し=対象外コマンドでの契約面書き込み事例・誤 deny 頻発・worker への構造化編集ツール付与。cargo 342・clippy 0・実機 hook バイナリで穴塞ぎ確認・対話 Codex end-to-end は次セッション
